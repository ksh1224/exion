# Migration `20200916065717-exion`

This migration has been generated by 권수현 at 9/16/2020, 3:57:17 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `exion`.`Shooting` DROP COLUMN `height`,
DROP COLUMN `weight`,
ADD COLUMN `minHeight` int  ,
ADD COLUMN `maxHeight` int  ,
ADD COLUMN `minweight` int  ,
ADD COLUMN `maxweight` int  
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200916064602-exion..20200916065717-exion
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "mysql"
-  url = "***"
+  url = "***"
 }
 model Profile {
   id                Int                 @default(autoincrement()) @id
@@ -35,17 +35,8 @@
   lastSignedIn      DateTime?
   profile           Profile?
 }
-model Schedule {
-  id                Int                 @default(autoincrement()) @id  
-  step              Step
-  shootingId        Int                 
-  userId            Int
-  User              User                @relation(fields: [userId], references: [id])
-  Shooting          Shooting            @relation(fields: [shootingId], references: [id])
-}
-
 model Shooting {
   id                Int                 @default(autoincrement()) @id   
   title             String?
   producer          String?
@@ -53,18 +44,29 @@
   shootingEndAt     DateTime?
   content           String?  
   wage              Int?
   gender            User_gender?        
-  height            Int?        
-  weight            Int?        
+  minHeight         Int?        
+  maxHeight         Int?        
+  minweight         Int?        
+  maxweight         Int?        
   isGlasses         Boolean?        
   isTattoo          Boolean?        
   isDyeing          Boolean? 
   createdAt         DateTime?           @default(now())
   updatedAt         DateTime?           @default(now()) @updatedAt
   deletedAt         DateTime?
 }
+model Schedule {
+  id                Int                 @default(autoincrement()) @id  
+  step              Step
+  shootingId        Int                 
+  userId            Int
+  User              User                @relation(fields: [userId], references: [id])
+  Shooting          Shooting            @relation(fields: [shootingId], references: [id])
+}
+
 model Bookmark {
   userId            Int                 @id
   shootingId        Int                 
   User              User                @relation(fields: [userId], references: [id])
```


